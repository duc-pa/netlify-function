{
  "name": "netlify-mongodb-example",
  "version": "1.0.0",
  "description": "An example showing how to connect to MongoDB using Netlify Functions",
  "scripts": {
    "build": "rm -rf build && mkdir build && cp index.html build/index.html"
  },
  "author": "@stephencookdev",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.5.8"
  }
}